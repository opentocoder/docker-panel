# Docker Panel 环境配置示例
# 复制此文件为 .env.local 并修改配置

# ===========================================
# JWT 密钥 (必须修改!)
# ===========================================
# 生成方式: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_RANDOM_32_CHAR_STRING

# ===========================================
# Docker 连接配置
# ===========================================

# 方式1: 本地 Unix Socket (推荐)
DOCKER_HOST=unix:///var/run/docker.sock

# 方式2: 远程 TLS 连接 (需要配置证书)
# DOCKER_HOST=tcp://192.168.1.100:2376
# DOCKER_TLS_VERIFY=1
# DOCKER_CERT_PATH=/path/to/docker/certs

# ⛔ 警告: 切勿使用未加密的 TCP 2375 端口!
# 这相当于给予任何人 root 权限
