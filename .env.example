# Docker Panel 环境配置示例
# 复制此文件为 .env.local 并修改配置

# ===========================================
# JWT 密钥 (必须修改!)
# ===========================================
# 生成方式: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_RANDOM_32_CHAR_STRING

# ===========================================
# Docker 连接配置 (可选)
# ===========================================
# 默认自动连接本地 /var/run/docker.sock，无需配置

# 如需连接远程 Docker，设置 DOCKER_SOCKET:
# DOCKER_SOCKET=tcp://192.168.1.100:2376

# ⛔ 警告: 切勿使用未加密的 TCP 2375 端口!
